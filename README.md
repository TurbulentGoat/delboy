# delboy
After accidentally deleting something (because I used rm instead of mv) I decided to make this. If you use "del &lt;path/file_name>" exactly as you would do with rm, it will move the file to the hidden /.trash folder wherever you set it up.
